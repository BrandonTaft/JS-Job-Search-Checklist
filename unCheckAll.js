
function unCheckAll() {
    //fires when Uncheck all button is clicked
    //sets answer to false and calls marker function
    var answer = false
    marker(answer);
}

    //fires when Uncheck all button is clicked
    //assigns a variable called marked to all inputs with name checklist
// function newFunction(answer) {
//     var marked = document.getElementsByName("checkList");
//     //iterates through all inputs and sets all to checked=false (unchecked)
//     for (var i = 0; i < marked.length; i++) {
//         marked[i].checked = answer;
//     }
// }

