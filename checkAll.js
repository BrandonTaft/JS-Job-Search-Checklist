function checkAll() {
    //fires when check all button is clicked
    //sets answer to true and calls marker function
    var answer = true
    marker(answer);
}


//fires when check all button is clicked
    //assigns a variable called marked to all inputs with name checklist
    // var markBox = document.getElementsByName("checkList");
    // for (var i = 0; i < markBox.length; i++) {
    //     if (markBox[i].type == "checkbox")
    //         markBox[i].checked = true;
    // }